# Host user UID/GID - must match the user running tmux on the host
# so the container can access the tmux socket
HOST_UID=1000
HOST_GID=1000

# Port exposed on the host (maps to container port 8000)
APP_PORT=8080

# Optional: Override tmux socket path if host uses a non-default location
# Default: /tmp/tmux-<uid>/default (auto-detected via volume mount)
# TMUX_SOCKET_PATH=/tmp/tmux-1000/default
